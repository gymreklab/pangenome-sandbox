# Exploring pangenome tools

Notes for fresh ubuntu install:

```
sudo apt-get update
sudo apt-get install emacs unzip

sudo mkdir /data
sudo chown -R ubuntu /data

git clone https://github.com/gymreklab/pangenome-sandbox
```
